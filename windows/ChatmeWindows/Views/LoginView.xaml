<Page
    x:Class="ChatmeWindows.Views.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="RootGrid" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" MaxWidth="400" Spacing="20">
            <TextBlock x:Name="TitleText" Text="Chatme Login" FontSize="32" FontWeight="Bold" HorizontalAlignment="Center"/>
            <TextBlock x:Name="SubtitleText" Text="Welcome" HorizontalAlignment="Center" />
            
            <StackPanel x:Name="ChoosePanel" Spacing="10">
                <Button Content="Google" HorizontalAlignment="Stretch" Click="GoogleLogin_Click"/>
                <Button Content="Email" HorizontalAlignment="Stretch" Click="ShowEmailInput_Click"/>
            </StackPanel>

            <StackPanel x:Name="EmailInputPanel" Spacing="10" Visibility="Collapsed">
                <TextBox x:Name="EmailBox" Header="Email" PlaceholderText="email@example.com"/>
                <Button Content="Send Code" HorizontalAlignment="Stretch" Click="SendCode_Click"/>
                <Button Content="Back" HorizontalAlignment="Center" Click="BackToChoose_Click"/>
            </StackPanel>

            <StackPanel x:Name="CodeVerifyPanel" Spacing="10" Visibility="Collapsed">
                <TextBlock x:Name="CodeEmailText" Text="Code sent to ..."/>
                <PasswordBox x:Name="CodeBox" Header="Verification Code"/>
                <Button Content="Login" HorizontalAlignment="Stretch" Click="VerifyCode_Click"/>
                <Button Content="Resend" HorizontalAlignment="Center" Click="ResendCode_Click"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Spacing="10" HorizontalAlignment="Center">
                <DropDownButton x:Name="LangButton" Content="EN" />
                <Button x:Name="ThemeToggleButton" Content="Theme" Click="ThemeToggle_Click" />
            </StackPanel>

            <TextBlock x:Name="ErrorDisplay" Foreground="Red" Visibility="Collapsed" HorizontalAlignment="Center" TextWrapping="Wrap"/>
        </StackPanel>

        <Grid x:Name="LoadingOverlay" Background="#80000000" Visibility="Collapsed">
            <ProgressRing IsActive="True" VerticalAlignment="Center" HorizontalAlignment="Center" />
        </Grid>

        <!-- No VisualStateManager for now to isolate issues -->
    </Grid>
</Page>
